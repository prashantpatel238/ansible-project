---
- hosts: all
  become: true

  tasks:
    - name: Make sure openssl and openssh are updted
      ansible.builtin.apt:
        name: "{{ item }}"
        state: latest
      loop:
        - openssl
        - openssh
    - name: Check if docker is installed
      ansible.builtin.command: docker --version